[build-system]
requires = ["setuptools","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calculator-demo"
version = "0.1.0"
description = "Simple calculator for CI/CD demo"
authors = [
    {name = "JuliaZ"}
]
dependencies = []